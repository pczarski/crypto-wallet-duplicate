import React from 'react';

export default function CurrencyBox(props) {

    return(
        <div>
            TODO: currency box that allows you to select currency, specify amount, or displays given amount
        </div>
    )
}